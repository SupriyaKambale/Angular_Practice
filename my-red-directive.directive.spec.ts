import { MyRedDirectiveDirective } from './my-red-directive.directive';

describe('MyRedDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MyRedDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
